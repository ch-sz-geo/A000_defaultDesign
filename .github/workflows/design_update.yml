
name: Trigger Repos

on:
  #workflow_run: 
  #  workflows: [pages-build-deployment]
  #  types:
  #    - completed
  #  branches: [main, master]
    
  workflow_dispatch:
  
jobs:
  trigger_Pages_Publish_in_other_Repo:
    runs-on: ubuntu-latest
    steps:
      - name: Generate GitHub App Token
        id: generate-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}  # scopes token to whole org

          
      - name: A254
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/ch-sz-geo/A254/dispatches \
            -d '{"event_type":"update-design","client_payload":{"ref":"main"}}'    

      - name: A256
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/ch-sz-geo/A256/dispatches \
            -d '{"event_type":"update-design","client_payload":{"ref":"main"}}'

      - name: A064
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/ch-sz-geo/A064/dispatches \
            -d '{"event_type":"update-design","client_payload":{"ref":"main"}}'

      - name: A027
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/ch-sz-geo/A027/dispatches \
            -d '{"event_type":"update-design","client_payload":{"ref":"main"}}'

      - name: A026
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/ch-sz-geo/A026/dispatches \
            -d '{"event_type":"update-design","client_payload":{"ref":"main"}}'

      - name: A180
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/ch-sz-geo/A180/dispatches \
            -d '{"event_type":"update-design","client_payload":{"ref":"main"}}'

      - name: A117
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/ch-sz-geo/A117/dispatches \
            -d '{"event_type":"update-design","client_payload":{"ref":"main"}}'

      - name: A259
        env:
          GH_TOKEN: ${{ steps.generate-token.outputs.token }}
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/A259/dispatches \
            -d '{"event_type":"update-design","client_payload":{"ref":"main"}}'
